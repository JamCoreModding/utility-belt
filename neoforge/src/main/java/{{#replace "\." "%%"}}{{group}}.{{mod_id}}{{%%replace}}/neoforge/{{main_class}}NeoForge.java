package {{group}}.{{mod_id}}.neoforge;

import {{group}}.{{mod_id}}.{{main_class}};
import net.neoforged.fml.common.Mod;

@Mod({{main_class}}.MOD_ID)
public class {{main_class}}NeoForge {
    public {{main_class}}NeoForge() {
        {{main_class}}.init();
    }
}
